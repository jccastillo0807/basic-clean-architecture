##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: demo-clean-architecture
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
